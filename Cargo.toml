[package]
name = "adams_leaf"
version = "0.1.0"
authors = ["DavidYo <lturtsamuel@gmail.com>"]
edition = "2018"

[dependencies]
rand = "0.5.5"
regex = "1"
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }

[lib]
doctest = false

[features]
batch-eval = []